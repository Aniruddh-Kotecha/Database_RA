LOAD ADVISOR
LOAD STUDENTS
TEMP <- SELECT adv_age > 40 FROM ADVISOR
TEMP1 <- CROSS STUDENTS, TEMP
TEMP2 <- SELECT adv_id == advisor_id FROM TEMP1
RESULT_G <- PROJECT stud_id, adv_id FROM TEMP2
PRINT RESULT_G
CLEAR ADVISOR
CLEAR STUDENTS
CLEAR TEMP
CLEAR TEMP1
CLEAR TEMP2